// user can sign up or login
// update his profile with with profilePicture  (will have a default picture untill updates his picture)
// can post Image with caption (post)
// can comment on any post
// can like a post
// can follow other user
// can create a story


// extra endpoints
// followers (all followers of a user && it's count)
//1. followers/user/:id    2./followers/user/:id/get/count

// stories (user's all stories && it's count)
//1. /stories/user/:id    2./stories/user/:id/get/count

// sory-views (all views on a story && it's count)
//1. /stories/story/:id    2./stories/story/:id/get/count



